import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.choijunhuk.sigilweaver',
  appName: 'SIGILWEAVER',
  webDir: 'dist',
};

export default config;
